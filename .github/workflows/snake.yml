name: generate animation

on:
  # run automatically every 24 hours
  schedule:
    - cron: "0 */24 * * *"
  
  # allows to manually run the job at any time
  workflow_dispatch:
  
  # run on every push on the main branch
  push:
    branches:
    - main
    
  

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      # Fetch contributions in 2020
      - name: Get contributions in 2020
        run: |
          # Write a script to fetch your contributions in 2020 using GitHub API
          # After fetching, format the data in a way that you can integrate it with your snake animation
          # For example, you might need to modify the SVG file generated by Platane/snk to include your 2020 contributions
          # Save the modified SVG file to dist folder
          echo "Script to fetch 2020 contributions and integrate with snake animation"
        working-directory: ${{ github.workspace }}

      # Generate snake game animation for 2020
      - name: generate github-contribution-grid-snake-2020.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake-2020.svg
            dist/github-contribution-grid-snake-2020-dark.svg?palette=github-dark
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      # Fetch contributions in 2021
      - name: Get contributions in 2021
        run: |
          # Write a script to fetch your contributions in 2021 using GitHub API
          # After fetching, format the data in a way that you can integrate it with your snake animation
          # For example, you might need to modify the SVG file generated by Platane/snk to include your 2021 contributions
          # Save the modified SVG file to dist folder
          echo "Script to fetch 2021 contributions and integrate with snake animation"
        working-directory: ${{ github.workspace }}

      # Generate snake game animation for 2021
      - name: generate github-contribution-grid-snake-2021.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake-2021.svg
            dist/github-contribution-grid-snake-2021-dark.svg?palette=github-dark
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Fetch contributions in 2022
      - name: Get contributions in 2022
        run: |
          # Write a script to fetch your contributions in 2022 using GitHub API
          # After fetching, format the data in a way that you can integrate it with your snake animation
          # For example, you might need to modify the SVG file generated by Platane/snk to include your 2022 contributions
          # Save the modified SVG file to dist folder
          echo "Script to fetch 2022 contributions and integrate with snake animation"
        working-directory: ${{ github.workspace }}

      # Generate snake game animation for 2022
      - name: generate github-contribution-grid-snake-2022.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake-2022.svg
            dist/github-contribution-grid-snake-2022-dark.svg?palette=github-dark
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Fetch contributions in 2023
      - name: Get contributions in 2023
        run: |
          # Write a script to fetch your contributions in 2023 using GitHub API
          # After fetching, format the data in a way that you can integrate it with your snake animation
          # For example, you might need to modify the SVG file generated by Platane/snk to include your 2023 contributions
          # Save the modified SVG file to dist folder
          echo "Script to fetch 2023 contributions and integrate with snake animation"
        working-directory: ${{ github.workspace }}

      # Generate snake game animation for 2023
      - name: generate github-contribution-grid-snake-2023.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake-2023.svg
            dist/github-contribution-grid-snake-2023-dark.svg?palette=github-dark
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Fetch contributions in 2024
      - name: Get contributions in 2024
        run: |
          # Write a script to fetch your contributions in 2024 using GitHub API
          # After fetching, format the data in a way that you can integrate it with your snake animation
          # For example, you might need to modify the SVG file generated by Platane/snk to include your 2024 contributions
          # Save the modified SVG file to dist folder
          echo "Script to fetch 2024 contributions and integrate with snake animation"
        working-directory: ${{ github.workspace }}

      # Generate snake game animation for 2024
      - name: generate github-contribution-grid-snake-2024.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake-2024.svg
            dist/github-contribution-grid-snake-2024-dark.svg?palette=github-dark
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      # Push the content of <build_dir> to a branch
      - name: Push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TO
